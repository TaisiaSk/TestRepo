#include <iostream>

int main()
{
	std::cout << "Enter the direction of the snake(right or down): ";
	bool
	/*const int Rows = 4;
	const int Cols = 6;

	int snake[Rows][Cols];
	int digit = 1;
	for (size_t rows = 0; rows < Rows; ++rows)
	{
		if (rows % 2 == 0)
			for (size_t cols = 0; cols < Cols; ++cols)
				snake[rows][cols] = digit++;

		else
			for (int cols = Cols - 1; cols >= 0; --cols)
				snake[rows][cols] = digit++;

	}

	for (size_t rows = 0; rows < Rows; ++rows)
	{
		for (size_t cols = 0; cols < Cols; ++cols)
			std::cout << snake[rows][cols] << ' ';

		std::cout << '\n';
	}*/

	system("pause");
	return(0);
}